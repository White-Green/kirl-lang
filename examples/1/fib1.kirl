import std::{io::println, num::{_add, _sub, _gt}};

// n番目のフィボナッチ数を求める
fn fib(n: Number) -> Number {
    if n < 2 {
        return n;
    } else {
        return fib(n - 1) + fib(n - 2);
    };
}

println(fib(20));
